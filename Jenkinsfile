#!groovy

node {
    
	
	
	stage "Prepare environment"
        checkout scm

	    stage "Checkout and build deps"
	    sleep 30

	    stage "Test and validate"
	    sleep 30

	    stage "Test and validate"
	    sleep 30


    stage "Cleanup"
        deleteDir()
}
